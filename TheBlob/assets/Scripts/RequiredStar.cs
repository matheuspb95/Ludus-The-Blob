using UnityEngine;
using System.Collections;

public class RequiredStar : MonoBehaviour {
	public int StarOrder;
	// Use this for initialization
	void Start () {
		if (StarsScore.Required == StarOrder);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
